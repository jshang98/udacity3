{
    "scripts": {
        "frontend:install": "cd reactnd-contacts-complete && npm install && npm uninstall -g macaddress",
        "backend:install": "cd reactnd-contacts-server && npm install && npm install -g yarn",
        "frontend:build": "cd reactnd-contacts-complete && npm run build",
        "backend:build": "cd reactnd-contacts-server && npm run build"
    }
}