{
    "scripts": {
        "frontend:install": "cd reactnd-contacts-complete && npm install got && npm install",
        "backend:install": "cd reactnd-contacts-server && npm install && npm install -g yarn",
        "frontend:build": "cd reactnd-contacts-complete && npm uninstall macaddress && npm run build",
        "backend:build": "cd reactnd-contacts-server && npm run build"
    }
}